---
# Shared assets for collections are handled via the rollout pipeline.
# Downstream repositories include this file to keep the structure consistent.
# No jobs are defined here; pipelines remain empty.
stages: []
